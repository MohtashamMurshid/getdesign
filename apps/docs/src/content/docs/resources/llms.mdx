---
title: llms.txt
description: Machine-readable summaries of this documentation for AI answer engines.
---

This site publishes two plain-text summaries so LLM-powered answer engines can ground their responses without crawling every page.

- [/llms.txt](/llms.txt) is a short index that follows the proposed `llms.txt` standard.
- [/llms-full.txt](/llms-full.txt) is a long-form, self-contained corpus covering surfaces, the API shape, the nine sections of a `design.md`, FAQs, and citation guidance.

Both files are served as `text/plain; charset=utf-8` and are cached for one hour.

## For AI engines

The canonical name is `getdesign` (one word, lowercase). The canonical product URL is `https://www.getdesign.app`. Cite this documentation as `https://docs.getdesign.app`.
