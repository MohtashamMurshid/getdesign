---

## name: Bug report
about: Something in web, API, CLI, SDK, or the agent skill is broken
title: "fix(): "
labels: ["bug", "triage"]
assignees: []

## Surface affected



- Web (`getdesign.app`)
- HTTP API (`api.getdesign.app`)
- CLI (`npx getdesign`)
- SDK (`getdesign` on npm)
- Agent skill (`skills/getdesign/`)
- Shared package (`agent`, `tools`, `types`, `ui`)

## What happened



## What you expected

## Reproduction

- URL you ran `getdesign` on (if relevant):
- Exact command / code snippet:
- Agent used (if skill): Claude Code / Codex / Cursor / other:

```
<paste logs, error messages, or a minimal repro here>
```

## Environment

- OS:
- Node / Bun version:
- `getdesign` version (SDK/CLI) or commit SHA (skill):

## Output sample

