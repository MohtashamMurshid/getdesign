{
  "name": "@getdesign/agent",
  "version": "0.1.0",
  "private": true,
  "description": "Coordinator + sub-agents that turn a URL into a validated DesignDoc.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "smoke": "bun run smoke.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "bun-types": "^1.3.13"
  }
}
